#keep going
MAKEFLAGS=-k

all:
	$(MAKE) ARCH=$(ARCH) CROSS_COMPILE=$(CROSS_COMPILE) -C $(AFI_KERNEL_HOME) SUBDIRS=$(CURDIR) modules

clean:
	$(MAKE) ARCH=$(ARCH) CROSS_COMPILE=$(CROSS_COMPILE) -C $(AFI_KERNEL_HOME) SUBDIRS=$(CURDIR) clean
